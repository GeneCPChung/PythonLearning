for pal in range(101, 999):
    #compare first number and last number
    #convert and int to a string
    pal = str(pal)

    print(pal)

# take loop from range of 999 DOWN TO 1 and compare palindrome
# if palindrome = yes
#     add to variable
# else ''

#slice in half and then compare

# loop from range in (100, 1000)
# for second in range(first, 1000):
#     product = first * second
#     product = str(product)
#     productrev = productstr[::-1]
#     if productstr = productrev
#         self.palindrome_list.append(product)